<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 스크린타임 분석: 사용자가 설정 > 사용 정보 접근에서 직접 허용한다 -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <!-- 홈 런처를 사용량에서 빼려고 기본 홈 앱만 조회한다 (QUERY_ALL_PACKAGES 는 쓰지 않는다) -->
    <queries>
      <intent>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.HOME" />
      </intent>
    </queries>
    <!-- 반려동물 사진 첨부: 카메라 촬영용. 앨범은 시스템 사진 선택기를 써서 권한이 필요 없습니다. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 펫 오버레이: 숏폼 앱 위에 펫을 띄운다 (설정 > 다른 앱 위에 표시에서 사용자가 허용) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 앱을 닫아도 숏폼 감지를 이어가는 포그라운드 서비스 + 그 상태 알림 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 오래 보면 펫이 커지면서 진동 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <service
        android:name=".overlay.OverlayService"
        android:exported="false"
        android:foregroundServiceType="specialUse">
        <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="Shows the user's virtual pet on top of short-form video apps to encourage taking a break" />
      </service>
    </application>
</manifest>
