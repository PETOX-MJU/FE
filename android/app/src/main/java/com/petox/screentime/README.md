# 스크린타임 분석기 (Kotlin)

`PETOX-MJU/AI` 저장소 `kotlin_port/src/main/kotlin/com/petox/screentime/` 의 **복사본**이다.
여기서 직접 고치지 말고 AI 저장소에서 고친 뒤(Python 기준 구현과 parity 테스트 통과) 다시 복사한다.

앱 쪽 연결 코드는 `com/petoxmju/petox/screentime/` 에 있다.
