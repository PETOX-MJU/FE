/**
 * 에셋 링크 설정. `npx react-native-asset` 을 실행하면
 * assets/fonts 의 .ttf 가 android/app/src/main/assets/fonts 로 복사됩니다.
 */
module.exports = {
  project: {
    android: {},
  },
  assets: ['./assets/fonts'],
};
